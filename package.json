{
  "name": "system-nominal-game",
  "version": "1.0.0",
  "description": "Orbital Physics RPG / Sandbox / Trading Simulator",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
